# Sagittarius Engine — Agent Entry Point

**Read [`ONBOARDING.md`](ONBOARDING.md) next.** It is the real process map: repository
layout, how context/rules/skills are routed, and the standing engineering discipline for this
repo. This file exists only because `AGENTS.md` is the filename many AI tools discover
automatically by convention — its job is to point somewhere real, nothing more.

Do not add content here. If something belongs at the entry point, it belongs in
`ONBOARDING.md`; if it's a policy, it belongs under `rules/`; if it's project knowledge, it
belongs under `context/`. A second copy of either drifts from the original the first time one
of them is edited and the other isn't — see `rules/doc-code-sync.md` for why that is exactly
the failure this repo is actively fixing (`Tasks/epics/EPIC-002_engine_sample_app_and_doc_rewrite/`).
